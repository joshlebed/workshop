---
description: Advance the Workshop.dev redesign by one chunk via the continue-redesign skill
---

Invoke the `continue-redesign` skill via the Skill tool now. Do not read or summarize the
skill's contents inline — call the tool so the skill runs as designed. The skill owns the
full procedure (chunk selection, execution, plan updates, PR + merge).
