# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo
.next/
.expo/
.expo-shared/
web-build/

# Env + secrets (never commit)
.env
.env.local
.env.*.local
*.pem
*.p8
*.p12
*.key
.aws/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
*.tfvars.local
!*.tfvars.example
infra/.placeholder.zip

# Logs
*.log
dev.log
logs/

# IDE
.vscode/
!.vscode/extensions.json
.idea/
*.swp
.DS_Store

# Expo
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Lambda build artifacts
apps/backend/dist/
apps/backend/*.zip

# Test
coverage/
.nyc_output/
test-results/
playwright-report/
/playwright/.cache/

# Local plans — keep off git so they can hold account IDs, tokens, and
# in-progress migration state without risk. HANDOFF.md is the one
# exception: it's the tracked cross-phase baton that names pending
# setup work (no secrets, just pointers).
docs/plans/*
!docs/plans/HANDOFF.md
